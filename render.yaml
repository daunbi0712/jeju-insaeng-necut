services:
  - type: web
    name: jeju-insaeng-necut
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_APPS_SCRIPT_URL
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
